import { SceneContainer } from '@/components/scene-container';

export default function Home() {
  return (
    <main className="relative w-screen h-screen overflow-hidden bg-[hsl(228,38%,5%)]">
      <SceneContainer />
    </main>
  );
}
